name: scrub-gate

on:
  push:
  pull_request:

jobs:
  scrub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scrub check (no keys/hashes may ship)
        run: bash scripts/scrub-check.sh "$GITHUB_WORKSPACE"
