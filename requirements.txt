pyyaml>=6.0
pydantic>=2.0
httpx>=0.27
sqlite-utils>=3.35
duckduckgo-search>=5.0
beautifulsoup4>=4.12
fastapi>=0.111
uvicorn>=0.30
psutil>=5.9
playwright>=1.40      # web_agent browser-automation skill (also run: playwright install chromium)
# Voice deps (speechrecognition, pyaudio, pyttsx3) are intentionally NOT here —
# pyaudio has no prebuilt wheel and needs a C compiler. They live in the
# optional "voice" extra (pip install -e '.[voice]'), installed best-effort by
# appliance/install-voice-stack.sh so voice can never break the core install.
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2
google-api-python-client>=2.100
